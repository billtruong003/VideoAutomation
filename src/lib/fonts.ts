/**
 * fonts.ts — self-hosted hand-drawn faces.
 *
 * Fonts ship from node_modules (@fontsource, OFL-1.1) rather than a CDN, so renders are
 * offline-safe, deterministic, and can never fall back to a system face mid-render.
 * See ASSET_LICENSES.md.
 */

import '@fontsource/bangers/latin-400.css';
import '@fontsource/patrick-hand/latin-400.css';
