# Normalise to LF in the repository. Without this, working on Windows rewrites every
# text file to CRLF and produces whole-file diffs that hide the real change.
* text=auto eol=lf

# Binary — never touch, never try to diff
*.mp3 binary
*.wav binary
*.mp4 binary
*.png binary
*.woff binary
*.woff2 binary
